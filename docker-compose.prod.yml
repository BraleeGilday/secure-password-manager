services:
  backend:
    build: ./backend
    env_file:
      - .env.prod
    environment:
      - DATABASE_URL=postgresql://${RDS_USER}:${RDS_PASSWORD}@${RDS_HOST}:${RDS_PORT}/${POSTGRES_DB}
    restart: always

  frontend:
    build: ./app
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always